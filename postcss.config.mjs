/** @type {import('postcss-load-config').Config} */
const config = {
  plugins: {
    // Tailwind CSS v4 with CSS-first configuration
    // The @import "tailwindcss" in CSS handles most of the processing
    tailwindcss: {},
  },
};

export default config;
