# Vercel build configuration to handle lockfile mismatch during Tailwind v4 migration

# Ignore the old lockfile to allow fresh dependency resolution
pnpm-lock.yaml

# Temporary files that might cause build conflicts
.next
node_modules
dist
build
